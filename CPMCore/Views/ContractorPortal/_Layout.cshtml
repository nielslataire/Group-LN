@using CPMCore.Helpers
@{
    var displayName = User.GetCpmDisplayName() ?? "";
    var initials    = string.Concat(displayName.Split(' ').Where(w => w.Length > 0).Take(2).Select(w => w[0])).ToUpper();
    var activeCtrl  = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
    var today       = DateTime.Today;
    var dayNames    = new[] { "zondag","maandag","dinsdag","woensdag","donderdag","vrijdag","zaterdag" };
    var monthNames  = new[] { "","januari","februari","maart","april","mei","juni","juli","augustus","september","oktober","november","december" };
    var dateStr     = $"{dayNames[(int)today.DayOfWeek]} {today.Day} {monthNames[today.Month]} {today.Year}";
}
<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="@Url.Content("~/img/favicon.ico")" type="image/x-icon" />
    <title>@(ViewData["Title"] != null ? $"{ViewData["Title"]} – " : "")Aannemersportaal</title>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800" rel="stylesheet" />
    <link rel="stylesheet" href="~/lib/bootstrap/css/bootstrap.css" />
    <link rel="stylesheet" href="~/lib/boxicons/css/boxicons.min.css" />
    <link rel="stylesheet" href="~/lib/font-awesome/css/all.min.css" />
    <link rel="stylesheet" href="~/css/contractor-portal.css" />
    @RenderSection("PageStyle", required: false)
</head>
<body>
<div class="gl-cp-root">

    <!-- ── Navigation ──────────────────────────────────────── -->
    <nav class="gl-cp-nav">
        <a href="/Werfportaal" class="gl-cp-nav-brand">
            <img src="~/img/logo.png" alt="Group LN" class="gl-cp-nav-logo-img" />
            <div class="gl-cp-nav-brand-text">
                <div class="gl-cp-nav-brand-name">GROUP LN</div>
                <div class="gl-cp-nav-brand-sub">AANNEMERSPORTAAL</div>
            </div>
        </a>

        <div class="gl-cp-nav-center" id="cpNavCenter">
            <div class="gl-cp-nav-links" id="cpNavLinks">
                <a href="/Werfportaal/Dashboard"
                   class="gl-cp-nav-link @(activeCtrl == "Dashboard" ? "active" : "")">
                    <i class="bx bx-grid-alt"></i> Dashboard
                </a>
                <a href="/Werfportaal/Werven"
                   class="gl-cp-nav-link @(activeCtrl is "Werven" or "WerfDetail" ? "active" : "")">
                    <i class="bx bx-buildings"></i> Werven
                </a>
            </div>
        </div>

        <div class="gl-cp-nav-right">
            <span class="gl-cp-nav-date">@dateStr</span>
            <div class="gl-cp-nav-user">
                <div class="gl-cp-nav-avatar">
                    <img src="/Account/ProfilePhoto" alt="@displayName"
                         onerror="this.style.display='none';this.nextElementSibling.style.display='flex'" />
                    <span class="gl-cp-hidden">@initials</span>
                </div>
                <span class="gl-cp-nav-username">@displayName</span>
            </div>
            <a href="/Account/SignOut" class="gl-cp-nav-signout" title="Afmelden">
                <i class="bx bx-log-out"></i>
            </a>
        </div>

        <button class="gl-cp-nav-toggle" id="cpNavToggle" aria-label="Menu">
            <i class="bx bx-menu"></i>
        </button>
    </nav>

    <!-- ── Body ────────────────────────────────────────────── -->
    <main class="gl-cp-body">
        @RenderBody()
    </main>

    <!-- ── Footer ──────────────────────────────────────────── -->
    <footer class="gl-cp-footer">
        <span>© @today.Year Group LN &middot; Aannemersportaal</span>
        <span>Vragen? <a href="mailto:info@groupln.be">info@groupln.be</a></span>
    </footer>

</div>

<script src="~/lib/jquery/jquery.js"></script>
<script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
<script src="~/js/contractor-portal.js"></script>
@RenderSection("PageScripts", required: false)
</body>
</html>

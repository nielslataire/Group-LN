@using CPMCore.Helpers
@using Microsoft.AspNetCore.Authorization
@using System.Linq

@{
	var userDisplayName = User.GetCpmDisplayName() ?? string.Empty;
	var userEmail = User.GetCpmEmail() ?? string.Empty;
	var userPhotoUrl = Url.Action("ProfilePhoto", "Account") ?? Url.Content("~/img/!logged-user.jpg");
	var userInitials = string.Join(
		string.Empty,
		userDisplayName
			.Split(' ', StringSplitOptions.RemoveEmptyEntries)
			.Select(part => char.ToUpperInvariant(part[0]))
			.Take(2));
	if (string.IsNullOrWhiteSpace(userInitials))
	{
		userInitials = "?";
	}
	var pageTitle = ViewData["Title"] as string ?? string.Empty;
	var pageIcon = ViewData["PageIcon"] as string ?? "bx bx-file";
	var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
	var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
	var isDashboard = string.Equals(currentController, "Home", StringComparison.OrdinalIgnoreCase)
		&& string.Equals(currentAction, "Index", StringComparison.OrdinalIgnoreCase);
}
<!doctype html>
<html lang="nl" class="modern fixed sidebar-light @ViewBag.sidebarcollapsed">
<head>

	<!-- Basic -->
	<meta charset="UTF-8">
	<title>@(string.IsNullOrWhiteSpace(pageTitle) ? "CPM - GROUP LN" : pageTitle + " · CPM - GROUP LN")</title>
	<link rel="icon" href="@Url.Content("~/img/favicon.ico")" type="image/x-icon" />
	<!-- Mobile Metas -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />

	<!-- Web Fonts -->
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link rel="preconnect" href="https://cdn.boxicons.com" crossorigin>
	<link rel="dns-prefetch" href="https://cdn.jsdelivr.net">
	<link href="https://fonts.googleapis.com/css?family=Poppins:300,400,500,600,700,800|Shadows+Into+Light&display=swap" rel="stylesheet" type="text/css">

	<!-- Vendor CSS -->
	<link rel="stylesheet" href="~/lib/bootstrap/css/bootstrap.min.css" />
	<link rel="stylesheet" href="~/lib/animate/animate.compat.css">
	<link rel="stylesheet" href="~/lib/font-awesome/css/all.min.css" />
@* 	<link rel="stylesheet" href="~/lib/boxicons/css/boxicons.min.css" /> *@
	<link href="https://cdn.boxicons.com/3.0.8/fonts/basic/boxicons.min.css" rel="stylesheet">
	<link rel="stylesheet" href="~/lib/magnific-popup/magnific-popup.css" />
	<link rel="stylesheet" href="~/lib/bootstrap-datepicker/css/bootstrap-datepicker3.css" />
	<link rel="stylesheet" href="~/lib/jquery-ui/jquery-ui.css" />
	<link rel="stylesheet" href="~/lib/jquery-ui/jquery-ui.theme.css" />
	<link rel="stylesheet" href="~/lib/bootstrap-multiselect/css/bootstrap-multiselect.css" />
	<link rel="stylesheet" href="~/lib/morris/morris.css" />
	<link rel="stylesheet" href="~/lib/pnotify/pnotify.custom.css" />
	<link rel="stylesheet" href="~/lib/select2/css/select2.css" />
	<link rel="stylesheet" href="~/lib/select2-bootstrap-theme/select2-bootstrap.min.css" />


	<!-- Page specific css -->
	@RenderSection("PageStyle", required: false)

	<!-- Coachmarks -->
	<link rel="stylesheet" href="~/css/coachmarks.css" />
	
	<!-- GROUP LN CSS -->
@* 	<link rel="stylesheet" href="css/groupln.css" />
	<link rel="stylesheet" href="css/skins/skin.css" /> *@
	<!-- Theme CSS -->
	<link rel="stylesheet" href="~/css/theme.css" asp-append-version="true" />

	@* <link rel="stylesheet" href="~/css/layouts/modern.css" /> *@
	<link rel="stylesheet" href="~/css/modern.css" asp-append-version="true" />

	<!-- Skin CSS -->
	<link rel="stylesheet" href="~/css/skins/default.css" asp-append-version="true" />
	<link rel="stylesheet" href="~/css/skins/test.css" asp-append-version="true" />
	<link rel="stylesheet" href="~/css/skins/test-extension.css" asp-append-version="true" />

	<!-- Theme Custom CSS -->
	<link rel="stylesheet" href="~/css/custom.css" asp-append-version="true">
@* 	<link rel="stylesheet" href="css/groupln-custom.css" /> *@

	<!-- Head Libs -->
	<script src="~/lib/modernizr/modernizr.js"></script>


</head>
<body>
	<a class="skip-link" href="#main-content">Naar inhoud</a>
	<section class="body">

		<!-- start: header -->
		<header class="header page-topbar">
			<div class="logo-container">
				<button class="btn header-btn-collapse-nav toggle-sidebar-left d-lg-none" type="button" aria-label="Menu openen">
					<i class="fas fa-bars" aria-hidden="true"></i>
				</button>
			</div>

			<!-- start: pagina-context (icoon + titel + breadcrumb) -->
			<div class="topbar-page @(isDashboard ? "topbar-page-nobreadcrumb" : string.Empty)">
				<span class="topbar-page-icon"><i class="@pageIcon" aria-hidden="true"></i></span>
				<div class="topbar-page-meta">
					<p class="topbar-page-title">@pageTitle</p>
					@if (!isDashboard)
					{
						@await Component.InvokeAsync("Breadcrumbs", new { viewName = "Default" })
					}
				</div>
			</div>
			<!-- end: pagina-context -->

			<!-- start: user box -->
			<div class="header-right">


				<div id="userbox" class="topbar-userbox dropdown">
					<a href="#" class="topbar-userbox-toggle" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false" aria-label="Gebruikersmenu">
						<span class="profile-picture profile-picture-as-text">@userInitials</span>
						<div class="profile-info" data-lock-name="@userDisplayName" data-lock-email="@userEmail">
							<span class="name">Hallo, <strong>@userDisplayName</strong></span>
						</div>
						<i class="fas fa-chevron-down topbar-userbox-caret" aria-hidden="true"></i>
					</a>

					<ul class="dropdown-menu dropdown-menu-end topbar-userbox-menu">
						<li class="topbar-userbox-header">
							<span class="topbar-userbox-header-name">@userDisplayName</span>
							<span class="topbar-userbox-header-email">@userEmail</span>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="@Url.Action("MijnHandtekening", "Account")"><i class="bx bx-envelope" aria-hidden="true"></i> Mijn handtekening</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="@Url.Action("SignOut", "Account")"><i class="bx bx-arrow-out-right-square-half" aria-hidden="true"></i> Afmelden</a>
						</li>
					</ul>
				</div>
			</div>
			<!-- end: user box -->
		</header>
		<!-- end: header -->

		<div class="inner-wrapper">
			<!-- start: sidebar -->
			@await Html.PartialAsync("_LeftSidebarPartial")
			<!-- end: sidebar -->
			<main id="main-content" class="content-body" tabindex="-1">
			@RenderBody()
			</main>
		</div>

	</section>

	<!-- Vendor -->
	<script src="~/lib/jquery/jquery.min.js"></script>

	<script>
		if (!jQuery.browser) {
			jQuery.browser = {};
		}
	</script>

	<script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
	<script src="~/lib/bootstrap-datepicker/js/bootstrap-datepicker.min.js"></script>
	<script src="~/lib/bootstrap-datepicker/locales/bootstrap-datepicker.nl-BE.min.js"></script>
	<script src="~/lib/common/common.js"></script>
	<script src="~/lib/nanoscroller/nanoscroller.js"></script>
	<script src="~/lib/magnific-popup/jquery.magnific-popup.min.js"></script>
	<script src="~/lib/jquery-placeholder/jquery.placeholder.js"></script>
	<script src="~/lib/select2/js/select2.min.js"></script>
	<script src="/lib/select2/js/i18n/nl.js"></script>


	<!-- Page specific scripts -->
	@RenderSection("PageScripts", required: false)

	<!-- Specific Page Vendor -->


	<script src="~/lib/bootstrap-multiselect/js/bootstrap-multiselect.min.js"></script>
	<script src="~/lib/morris/morris.min.js"></script>
	<script src="~/lib/pnotify/pnotify.custom.js"></script>
	<script src="https://cdn.jsdelivr.net/npm/autonumeric@4.10.5/dist/autoNumeric.min.js"></script>

	<!-- Theme Base, Components and Settings -->
	<script src="~/js/theme.js" asp-append-version="true"></script>

	<!-- Theme Custom -->
	<script src="~/js/custom.js" asp-append-version="true"></script>

	<!-- Coachmarks -->
	@await Html.PartialAsync("_Coachmarks")
	<script src="~/js/coachmarks.js" asp-append-version="true"></script>
	<script src="~/js/notificaties.js" asp-append-version="true"></script>

	<!-- Theme Initialization Files -->
	<script src="~/js/theme.init.js" asp-append-version="true"></script>

	<!-- Examples -->
	<script src="~/js/examples/examples.dashboard.js"></script>
	<script src="~/js/examples/examples.modals.js"></script>
	<script>
		$(window).on('load', function () {
		@if (TempData["Message"] != null)
		{
			<text>
						new PNotify({
							title:    @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["MessageTitle"]?.ToString() ?? "")),
							text:     @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["Message"]?.ToString() ?? "")),
							type:     @Html.Raw(System.Text.Json.JsonSerializer.Serialize(TempData["MessageType"]?.ToString() ?? "info")),
							addclass: 'stack-bottomright'
						});
			</text>
		}
		});
	</script>
	<script>
		(function () {
			var $photo = $('.user-profile-photo');
			$photo.on('load', function () {
				$(this).siblings('.profile-picture-as-text').hide();
			});
			$photo.on('error', function () {
				$(this).hide();
				$(this).siblings('.profile-picture-as-text').show();
			});

			var $search = $('#header-search');
			if (!$search.length) {
				return;
			}

			$search.select2({
				theme: 'bootstrap',
				placeholder: $search.data('placeholder'),
				allowClear: true,
				minimumInputLength: 2,
				language: 'nl',
				ajax: {
					url: '@Url.Action("Lookup", "Search")',
					dataType: 'json',
					delay: 300,
					data: function (params) {
						return { term: params.term };
					},
					processResults: function (data) {
						return data;
					}
				},
				templateResult: function (item) {
					if (item.loading || item.children) {
						return item.text;
					}

					var $container = $('<div class="select2-result-item"></div>');
					$container.append($('<div class="fw-semibold"></div>').text(item.text));
					if (item.subtitle) {
						$container.append($('<div class="small text-muted"></div>').text(item.subtitle));
					}
					return $container;
				},
				templateSelection: function (item) {
					return item.text || item.id || '';
				},
				escapeMarkup: function (markup) {
					return markup;
				}
			});

			$search.on('select2:select', function (event) {
				var item = event.params.data;
				if (item && item.id) {
					window.location.href = item.id;
				}
			});
		})();
	</script>
	<script>
		 @if (User?.Identity?.IsAuthenticated ?? false)
		{
			<text>
					 (function () {
						const keepAliveUrl = '@Url.Action("KeepAlive", "Account")';
						if (!keepAliveUrl) {
							return;
						}

						const intervalMs = 5 * 60 * 1000; // 5 minuten
						let timeoutId = null;

						function queuePing() {
							if (timeoutId) {
								window.clearTimeout(timeoutId);
							}

							timeoutId = window.setTimeout(sendPing, intervalMs);
						}

						function sendPing() {
							fetch(keepAliveUrl, {
								method: 'GET',
								credentials: 'same-origin',
								cache: 'no-store',
								headers: {
									'X-Requested-With': 'XMLHttpRequest'
								}
							})
								.catch(function () { return null; })
								.then(queuePing);
						}

						document.addEventListener('visibilitychange', function () {
							if (document.visibilityState === 'visible') {
								queuePing();
							} else if (timeoutId) {
								window.clearTimeout(timeoutId);
								timeoutId = null;
							}
						});

						queuePing();
					})();
			
			</text>
			
		}
	</script>

</body>
</html>
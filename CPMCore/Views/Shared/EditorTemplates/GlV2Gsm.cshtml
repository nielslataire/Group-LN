@model string
@*
    gl-v2 (design-handoff optie 8f·2, "GSM"). Zelfde veld als GlV2Telefoon.cshtml, ander icoon en
    andere actie (Sms i.p.v. Bellen) — zoals de mockup se eigen toelichting het stelt. Enkel
    expliciete aanroep. Geen zichtbare standaard placeholder-tekst, wel een spatie als placeholder-
    attribuut — zie GlV2Telefoon.cshtml se eigen toelichting voor waarom (:placeholder-shown-CSS
    matcht nooit zonder een placeholder-attribuut, dus zonder dit zou "Sms" altijd zichtbaar zijn).
    Optionele extra ViewData: Placeholder.
*@
@{
    var name = ViewData.TemplateInfo.GetFullHtmlFieldName("");
    var id = name.Replace(".", "_");
    var label = ViewData.ModelMetadata.DisplayName ?? ViewData.ModelMetadata.PropertyName;
    var placeholder = ViewData["Placeholder"] as string ?? " ";
    var hasError = ViewContext.ModelState.TryGetValue(name, out var entry) && entry.Errors.Count > 0;
}
<div class="gl-v2-field@(hasError ? " is-error" : "")">
    <label class="gl-v2-field-label" for="@id">@label</label>
    <div class="gl-v2-field-box">
        <i class="ph ph-device-mobile" aria-hidden="true"></i>
        <span class="gl-v2-field-prefix">+32</span>
        <span class="gl-v2-field-divider"></span>
        <input type="tel" id="@id" name="@name" class="gl-v2-field-input"
               value="@Model" placeholder="@placeholder" autocomplete="off" />
        <a class="gl-v2-field-action" href="sms:@Model"><span>Sms</span></a>
    </div>
    @if (hasError)
    {
        <span class="gl-v2-field-help">@entry!.Errors[0].ErrorMessage</span>
    }
</div>

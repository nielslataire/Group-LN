@model String

<div class="input-group">
    <span class="input-group-text">
        <i class="bx bx-phone"></i>
    </span>
    @Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue, new { @class = "form-control form-control-modern Phonemask", placeHolder = ViewData.ModelMetadata.DisplayName})
</div>
@* Masking gebeurt centraal in de pagina-scripts (na het laden van jQuery + jquery.mask),
   bv. in Klanten/EditProject.cshtml → initCoOwnerItem. Een inline <script> hier draait vóór
   jQuery geladen is en gooit "jQuery is not defined". *@
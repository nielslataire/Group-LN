@model CPMCore.Models.EditUserViewModel
@{
    ViewData["Title"] = "Gebruiker bewerken";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="card">
    <header class="card-header">
        <h2 class="card-title">@ViewData["Title"]</h2>
        <p class="card-subtitle">Werk gegevens en rollen bij.</p>
    </header>
    <div class="card-body">
        @using (Html.BeginForm("Edit", "UserAdmin", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.Id)
            <div class="row mb-3">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.UserName)</label>
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="row mb-3">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.Email)</label>
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control", type = "email" })
                    @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="row mb-3">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.Name)</label>
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                </div>
            </div>
            <div class="row mb-3">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.Forename)</label>
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.Forename, new { @class = "form-control" })
                </div>
            </div>
            <div class="row mb-3">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.JobFunction)</label>
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.JobFunction, new { @class = "form-control" })
                </div>
            </div>
            <div class="row mb-4">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.Cellphone)</label>
                <div class="col-sm-9">
                    @Html.TextBoxFor(m => m.Cellphone, new { @class = "form-control" })
                </div>
            </div>

            <div class="row mb-4">
                <label class="col-sm-3 col-form-label text-sm-end">@Html.DisplayNameFor(m => m.SelectedRoles)</label>
                <div class="col-sm-9">
                    <div class="row">
                        @for (var i = 0; i < Model.AvailableRoles.Count; i++)
                        {
                            var role = Model.AvailableRoles[i];
                            <div class="col-sm-6 mb-2">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="SelectedRoles" value="@role" @(Model.SelectedRoles.Contains(role) ? "checked" : string.Empty) />
                                    <label class="form-check-label">@role</label>
                                </div>
                            </div>
                        }
                    </div>
                    @Html.ValidationMessageFor(m => m.SelectedRoles, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="text-end">
                <a class="btn btn-outline-secondary" href="@Url.Action("Index")">Annuleren</a>
                <button type="submit" class="btn btn-primary">Opslaan</button>
            </div>
        }
    </div>
</section>